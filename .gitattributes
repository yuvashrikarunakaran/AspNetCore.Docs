# Automatically normalize line endings for all text files
* text=auto

# Explicitly declare text files to always be normalized and converted
# to native line endings on checkout.
*.cs text
*.cshtml text

# Ensure solution files always use CRLF line endings on checkout.
*.sln text eol=crlf
